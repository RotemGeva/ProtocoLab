1.0.0.0 -
		Initial release of ProtocoLab.
1.0.1.0 -
		Added support of uploding a CSV as input.
1.0.1.1 -
		Added exit code 1 when CompareTool failes and fixed bug that MR name cannot contain period.
1.0.2.0 - 
		Added execution state column.
1.0.3.0 - 
		Added support for "run from selection" - user can now select certain rows to compare by using the checkboxes on the left.
		Added log folder to conatin all external tool logs and log name now contains MR name.
		Added log folder to contain all GUI logs and log file name is now ProtocoLab.
		Added "clean execution" before comparing - execution state restarts before every run.
		Added Open Latest Log - allows the user to open latest compare log.
		Added ability to make requirements from a given tar file.
		Enriched log file of both GUI and external tool.
		